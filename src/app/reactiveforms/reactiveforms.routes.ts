import { Routes } from "@angular/router";
import { ReactiveformsComponent } from "./reactiveforms.component";

export const reactiveForms: Routes = [
    { path: 'reactiveforms', 
      component: ReactiveformsComponent 
    }
  ];